<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            font-family: 'Merriweather', serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background-color: #fff; /* Change background color to white */
            color: #1a1a1a; /* Change text color to black */
        }

        header {
            background-color: #fff; /* Change header background color to white */
            color: #1a1a1a;
            padding: 10px;
            text-align: center;
        }

        header a{
            color: #1a1a1a;
            background-color: #8c6d50;
            padding: 10px;
            text-decoration: none;
            margin: 0 10px;
            border-radius: 5px;
            display: inline-block;
            float: right;
        }

        header img {
            position: absolute;
            top: 0px; /* Adjust the top position as needed */
            left: 0px; /* Adjust the left position as needed */
            width: 200px; /* Adjust the width as needed */
            height: auto;
        }

        h1, h2, h3 {
            font-family: 'Merriweather', serif;
            color: #1a1a1a; /* Black color */
        }

        h1 {
            font-size: 81px;
            margin: 0;
            font-weight: bold;
        }

        h3 {
           float: right; 
        }

        main {
            padding: 20px;
        }

        a {
            color: #000000;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        footer {
            background-color: #8c6d50;
            color: #fff;
            padding: 10px;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>
    <header>
        <img src="img/logo.png" alt="Logo">
        <h1>Grind & Co.</h1>
        <a href="/login">Login</a>
        <a href="/logout">Log out</a>
        {{#if username}}
            <h3 align="center">Signed in as: {{username}}</h3>
        {{/if}}
    </header>

    <main>
        <br>
        <h2 align="center"><a href="/listprod">Begin Shopping</a></h2>
        <h2 align="center"><a href="/listorder">List All Orders</a></h2>
        <h2 align="center"><a href="/customer">Customer Info</a></h2>
        <h2 align="center"><a href="/admin">Administrators</a></h2>
    </main>

    <footer>
        &copy; 2023 Grind & Co. . All rights reserved.
    </footer>
</body>
</html>